Generate a PR summary for the changes in the current feature branch, which aim to implement the following GitHub issue (use the `gh` CLI to get issue details):
$ARGUMENTS

Write the PR summary to a `.md` file.